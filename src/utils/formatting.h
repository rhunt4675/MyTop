#include <string>

using namespace std;

//Various utility functions used by MyTop
string bytesToReadable(long bytes);
string ticksToReadable(unsigned long long ticks);
string uptimeToReadable(double uptime);
